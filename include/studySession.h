#include <vector>
#include "flashcard.h"
#include "database.h"

class studySession{

    public:
        studySession(std::vector<Flashcard> &flashcardvector,std::string const &deckName);

    private:
        
};